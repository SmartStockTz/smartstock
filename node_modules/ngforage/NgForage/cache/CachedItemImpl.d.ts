import { CachedItem } from './CachedItem';
/** @internal */
export declare class CachedItemImpl<T> implements CachedItem<T> {
    readonly data: T;
    readonly expires: Date;
    constructor(data: T, expiryTime: number);
    readonly expired: boolean;
    readonly expiresIn: number;
    readonly hasData: boolean;
    toJSON(): CachedItem<T>;
    toString(): string;
}
