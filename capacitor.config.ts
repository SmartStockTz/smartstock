import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fahamutech.smartstock',
  appName: 'smartstock',
  webDir: 'dist/smartstock',
  bundledWebRuntime: false
};

export default config;
